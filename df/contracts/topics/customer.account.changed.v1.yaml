contractVersion: 1
topic:
  name: customer.account.changed.v1
  domain: customer
  product: crm-sales
  domainOwner: customer-domain-council
  ownerTeam: data-platform
  dataSteward: enterprise-data-steward
  sourceServices:
    - crm-account-service
    - sales-opportunity-service
  description: Tokenized CRM account and customer lifecycle signal used to build enterprise Customer 360 data products.
  status: DRAFT

schema:
  format: JSON_SCHEMA
  compatibility: BACKWARD_TRANSITIVE
  envelopeSchema: contracts/event-envelope.v1.schema.json
  payloadSchema: contracts/events/customer.account.changed.v1.schema.json

privacy:
  classification: PII
  dataResidency: REGION_CONTROLLED
  containsPii: true
  tenantIsolation: REQUIRED
  retentionDays: 1095
  erasureSupported: true
  subjectKeys:
    - name: customer_id_hash
      topicPath: $.payload.customerIdHash
      type: tokenized_subject_id
      required: true
  erasureMode: SUBJECT_DELETE
  legalHoldPolicy: standard_legal_hold_check
  rawPayloadPolicy: SECURE_RAW_PAYLOAD_WITH_SUBJECT_ERASURE

ingestion:
  bronzeTarget: bronze.events_customer_account_changed
  partitionStrategy: event_date/source_service/org_id

quality:
  freshnessSloMinutes: 30
  checks:
    - name: event_id_not_null
      type: not_null
      column: eventId
    - name: customer_account_event_type_allowed
      type: accepted_values
      column: payload.eventType
      values: [CUSTOMER_ACCOUNT_CHANGED]
    - name: source_offset_unique
      type: unique
      columns: [topic, partition, offset]
    - name: no_sensitive_payload
      type: no_sensitive_payload
      column: payload
