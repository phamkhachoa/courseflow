terraform {
  required_version = ">= 1.7.0"
}

# Production runtime skeleton only.
# This file is not apply evidence. Production readiness still requires reviewed plan/apply output,
# service identity audit, secret manager policy, private network policy, backup evidence and a DR
# exercise report.
